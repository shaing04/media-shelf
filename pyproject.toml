[project]
name = "shelf"
version = "0.1.0"
description = "A personal media tracking CLI"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "click>=8.4.1",
]

[project.scripts]
shelf = "shelf.cli:main"
